id: G22
title: Security checklist
priority: P2
status: done
why: "Releases need a single human-readable checklist that mirrors the automated guards (fixture scan, redaction, MCP profile defaults)."
scope:
  allowed:
    - sdk/docs/SECURITY-CHECKLIST.md
    - sdk/tests/security-checklist.test.ts
  forbidden:
    - sdk/src/funcs/**
    - sdk/src/sdk/**
    - sdk/src/models/**
    - sdk/src/lib/**
    - sdk/src/hooks/**
    - sdk/src/types/**
    - sdk/src/core.ts
    - sdk/src/index.ts
acceptance:
  - "docs/SECURITY-CHECKLIST.md has Secrets, Webhooks, MCP, and CI sections."
  - "Property tests pin the checklist claims: auth-header, debug-redaction/serialized-error, replay fixture scan (prose-pin security-checklist.test.ts deleted 2026-08-10)."
commands:
  - "cd sdk && npm test -- tests/auth-header.test.ts tests/debug-redaction.test.ts tests/replay.test.ts"
  - "cd sdk && npm run goals:check"
adversarial_checks:
  - "Removing the ApiKey header or leaking key material through errors fails the property tests."
links:
  docs:
    - sdk/docs/SECURITY-CHECKLIST.md
  tests:
    - sdk/tests/auth-header.test.ts
    - sdk/tests/debug-redaction.test.ts
    - sdk/tests/replay.test.ts
rollback: "Revert this commit; automated guards remain in place."
