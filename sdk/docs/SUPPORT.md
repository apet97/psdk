# Support

Supported runtime: Node.js 20 or newer, ESM.

Primary supported surfaces are the raw generated SDK, facade helpers, curated
MCP tools, webhook helpers, and CLI.

Experimental surfaces are listed in `docs/STABILITY.md`.

## Browser support

The package is tested for Node.js 20 or newer. Browser use is not a supported
target unless a specific workflow has its own browser test.
