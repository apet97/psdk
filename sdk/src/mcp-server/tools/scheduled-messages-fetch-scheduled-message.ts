/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledMessagesFetchScheduledMessage } from "../../funcs/scheduled-messages-fetch-scheduled-message.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FetchScheduledMessageRequest$inboundSchema,
};

export const tool$scheduledMessagesFetchScheduledMessage: ToolDefinition<
  typeof args
> = {
  name: "scheduled-messages-fetch-scheduled-message",
  description: `Fetch a single scheduled message by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scheduledMessagesFetchScheduledMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
