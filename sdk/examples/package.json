{
  "name": "pumble-keys-sdk-examples",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:parent": "npm --prefix .. install --no-audit --no-fund && npm --prefix .. run build",
    "build:examples": "npm install --no-audit --no-fund",
    "build": "npm run build:parent && npm run build:examples"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "pumble-keys-sdk": "file:.."
  }
}
