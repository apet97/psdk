# Contributing

The SDK package lives in `sdk/`.

Start with `CONTEXT.md`, then read `sdk/CONTRIBUTING.md`.
Do not hand-edit generated SDK files. Change the OpenAPI spec, generation
config, or handwritten extensions instead.
